# Teutonic-Q3-8B — Qwen3-8B dense as genesis king (vanilla HF Qwen3ForCausalLM, no fork).
# Qwen3 dense family, same tokenizer/arch line as 4B, just a larger baseline.
# Public eval corpus: FineWeb-Edu Hippius mirror tokenized at eval time with Qwen3 tokenizer.
#
# Genesis now lives on Hippius Hub. model_store dispatches by digest prefix:
# `sha256:` for Hippius OCI, `hf:` for HF revisions. Challenger uploads still
# go through Hippius as normal.
[chain]
name = "Teutonic-Q3-8B"
seed_repo = "teutonic/teutonic-q3-8b-genesis"
# repo_pattern auto-derives to "^[^/]+/Teutonic-Q3-8B-.+$" if omitted.

[arch]
module = "archs.qwen3"
# Generic structural keys (vocab/hidden/heads/etc.) are enforced by
# validate_challenger_config automatically. Anything Qwen3-specific that we
# require challengers to preserve goes here.
extra_lock_keys = [
  "rope_theta",
  "rope_scaling",
  "tie_word_embeddings",
  "max_position_embeddings",
]

[seed]
tokenizer_repo = "Qwen/Qwen3-8B"
repo_backend = "hippius"
seed_digest = "sha256:97d3f4ac49ae94ccb9bb1a73ff68de6199150cb0346bcf16c16138387a19e7a4"
